# String 

word = "python"
word1 = "program"
print(len(word)) 
print(word + word1) # + is used to concanate strings 

for ch in word:
    print(ch)

print(word[1])
# python string are imutable (if once string is created we can't chage the string )